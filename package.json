{
  "name": "components-collection",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "node watch-components.js",
    "server": "browser-sync start --server --files \"./components/**/*, *.css, *.html, *.js, components.json\"",
    "start": "npm-run-all --parallel server watch"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
